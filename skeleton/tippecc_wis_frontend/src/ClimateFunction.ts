export class ClimateFunction {
	public id: number;
	public name: string;
	public description: string = "none"

    constructor(id: number, name:string) {
        this.id = id;
        this.name = name;
    }
	}
